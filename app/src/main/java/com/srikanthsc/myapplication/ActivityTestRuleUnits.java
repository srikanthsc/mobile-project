package com.srikanthsc.myapplication; //nom du package

// classe detiner pour effectuer les test unitaires
public class ActivityTestRuleUnits<T> {
    public ActivityTestRuleUnits(Class<T> mainActivityClass) {
    }

    public Class<? extends ActivityTestRuleUnits> getActivity() { // classe qui herite de la classe activityTestRuleUnits

        return getActivity(); // getActivity() renvoie l'activité à laquelle le fragment est actuellement associé.
    }
}
